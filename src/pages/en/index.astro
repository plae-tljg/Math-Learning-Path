---
import MathLayout from '../../layouts/MathLayout.astro';
import { getPath } from '../../utils/path';
import { t } from '../../utils/i18n';

const lang = 'en';
const currentPath = '/en/';

// Learning levels data
const levels = [
	{
		level: 0,
		name: 'Genesis',
		icon: '∅',
		subjects: ['Set Theory', 'Mathematical Logic'],
		color: '#6366f1',
	},
	{
		level: 1,
		name: 'First Collision',
		icon: '⚛',
		subjects: ['Number Theory', 'Basic Algebra'],
		color: '#8b5cf6',
	},
	{
		level: 2,
		name: 'Multi-Collision',
		icon: '⊕',
		subjects: ['Group Theory', 'Analysis'],
		color: '#ec4899',
	},
	{
		level: 3,
		name: 'Structure Collision',
		icon: '◈',
		subjects: ['Topology', 'Ring & Field'],
		color: '#f43f5e',
	},
	{
		level: 4,
		name: 'High-Dimension',
		icon: '∮',
		subjects: ['Complex Analysis', 'Measure'],
		color: '#f97316',
	},
	{
		level: 5,
		name: 'Unified Abstraction',
		icon: '∀',
		subjects: ['Category Theory'],
		color: '#eab308',
	},
];

const learningPaths = [
	{
		title: 'Getting Started',
		desc: 'Start from high school mathematics basics',
		href: '/en/getting-started',
		icon: '📚',
	},
	{
		title: 'University Math',
		desc: 'Systematic study of university mathematics',
		href: '/en/university',
		icon: '🎓',
	},
];
---

<MathLayout title={t('common.title', lang)} lang={lang} {currentPath}>
	<!-- Hero Section -->
	<section class="hero">
		<div class="hero-content">
			<div class="hero-icon">∑</div>
			<h1>{t('common.title', lang)}</h1>
			<p class="hero-subtitle">{t('common.subtitle', lang)}</p>
			<blockquote>
				<p>"Internalize all mathematical derivations in your mind. Different elements become monads, constantly colliding, producing different theorems."</p>
			</blockquote>
		</div>
	</section>

	<!-- Monad Theory Section -->
	<section class="section">
		<h2>{t('home.monad-theory', lang)}</h2>
		<div class="card">
			<p class="theory-text">{t('home.monad-desc', lang)}</p>
			<div class="theory-diagram">
				<div class="monad monad-1">
					<span>∅</span>
					<small>Empty Set</small>
				</div>
				<div class="collision">⟷</div>
				<div class="monad monad-2">
					<span>⟂</span>
					<small>True/False</small>
				</div>
				<div class="collision">⟷</div>
				<div class="monad monad-3">
					<span>ℕ</span>
					<small>Natural Numbers</small>
				</div>
				<div class="equals">=</div>
				<div class="result">
					<span>New Theorem</span>
				</div>
			</div>
		</div>
	</section>

	<!-- Learning Paths Section -->
	<section class="section">
		<h2>{t('home.learning-paths', lang)}</h2>
		<div class="paths-grid">
			{learningPaths.map(path => (
				<a href={getPath(path.href)} class="path-card card">
					<span class="path-icon">{path.icon}</span>
					<h3>{path.title}</h3>
					<p>{path.desc}</p>
					<span class="path-arrow">→</span>
				</a>
			))}
		</div>
	</section>

	<!-- Levels Section -->
	<section class="section">
		<h2>{t('home.levels', lang)}</h2>
		<p class="section-intro">Mathematics study is divided into six levels, from foundation to abstraction.</p>
		<div class="levels-timeline">
			{levels.map((level, index) => (
				<div class="level-item" style={`--level-color: ${level.color}`}>
					<div class="level-badge">
						<span class="level-icon">{level.icon}</span>
						<span class="level-num">L{level.level}</span>
					</div>
					<div class="level-content card">
						<h3>{level.name}</h3>
						<div class="level-subjects">
							{level.subjects.map(subject => (
								<span class="subject-tag">{subject}</span>
							))}
						</div>
					</div>
					{index < levels.length - 1 && <div class="level-connector"></div>}
				</div>
			))}
		</div>
	</section>

	<!-- About Section -->
	<section class="section">
		<h2>{t('home.about', lang)}</h2>
		<div class="card about-card">
			<p>{t('home.about-desc', lang)}</p>
		</div>
	</section>
</MathLayout>

<style>
	/* Hero Section */
	.hero {
		text-align: center;
		padding: 3rem 0;
		margin-bottom: 2rem;
	}

	.hero-content {
		max-width: 800px;
		margin: 0 auto;
	}

	.hero-icon {
		font-size: 4rem;
		margin-bottom: 1rem;
		background: linear-gradient(135deg, #6366f1, #ec4899);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		animation: pulse 3s ease-in-out infinite;
	}

	@keyframes pulse {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.05); }
	}

	.hero h1 {
		font-size: 3rem;
		margin-bottom: 0.5rem;
	}

	.hero-subtitle {
		font-size: 1.25rem;
		color: #94a3b8;
		margin-bottom: 2rem;
	}

	.hero blockquote {
		text-align: center;
		max-width: 600px;
		margin: 2rem auto;
	}

	/* Section Styles */
	.section {
		margin: 3rem 0;
	}

	.section-intro {
		margin-bottom: 1.5rem;
	}

	/* Theory Diagram */
	.theory-text {
		margin-bottom: 2rem;
		font-size: 1.1rem;
	}

	.theory-diagram {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: 1rem;
		flex-wrap: wrap;
		padding: 2rem;
		background: rgba(99, 102, 241, 0.05);
		border-radius: 12px;
		border: 1px solid rgba(99, 102, 241, 0.2);
	}

	.monad {
		display: flex;
		flex-direction: column;
		align-items: center;
		padding: 1rem 1.5rem;
		background: rgba(99, 102, 241, 0.1);
		border-radius: 12px;
		border: 2px solid rgba(99, 102, 241, 0.3);
	}

	.monad span {
		font-size: 2rem;
		font-weight: bold;
		color: #6366f1;
	}

	.monad small {
		font-size: 0.75rem;
		color: #94a3b8;
		margin-top: 0.25rem;
	}

	.collision {
		font-size: 1.5rem;
		color: #ec4899;
		animation: bounce 1s ease-in-out infinite;
	}

	@keyframes bounce {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-5px); }
	}

	.equals {
		font-size: 2rem;
		color: #8b5cf6;
	}

	.result {
		padding: 1rem 2rem;
		background: linear-gradient(135deg, #6366f1, #ec4899);
		border-radius: 12px;
	}

	.result span {
		font-size: 1.25rem;
		font-weight: bold;
		color: white;
	}

	/* Paths Grid */
	.paths-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.5rem;
	}

	.path-card {
		display: block;
		text-decoration: none;
		position: relative;
		overflow: hidden;
	}

	.path-card:hover .path-arrow {
		transform: translateX(5px);
	}

	.path-icon {
		font-size: 2.5rem;
		display: block;
		margin-bottom: 1rem;
	}

	.path-card h3 {
		margin: 0 0 0.5rem 0;
		color: #f1f5f9;
	}

	.path-card p {
		margin: 0;
		color: #94a3b8;
	}

	.path-arrow {
		position: absolute;
		top: 1.5rem;
		right: 1.5rem;
		font-size: 1.5rem;
		color: #6366f1;
		transition: transform 0.2s ease;
	}

	/* Levels Timeline */
	.levels-timeline {
		display: flex;
		flex-direction: column;
		gap: 0;
	}

	.level-item {
		display: flex;
		align-items: flex-start;
		gap: 1.5rem;
		position: relative;
	}

	.level-badge {
		display: flex;
		flex-direction: column;
		align-items: center;
		min-width: 80px;
	}

	.level-icon {
		font-size: 2rem;
		background: linear-gradient(135deg, var(--level-color), #8b5cf6);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.level-num {
		font-size: 0.75rem;
		font-weight: bold;
		color: var(--level-color);
		background: rgba(99, 102, 241, 0.1);
		padding: 0.25rem 0.5rem;
		border-radius: 4px;
		margin-top: 0.25rem;
	}

	.level-content {
		flex: 1;
		margin: 0;
	}

	.level-content h3 {
		margin: 0 0 0.75rem 0;
		color: #f1f5f9;
	}

	.level-subjects {
		display: flex;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.subject-tag {
		padding: 0.25rem 0.75rem;
		background: rgba(99, 102, 241, 0.2);
		border-radius: 20px;
		font-size: 0.8rem;
		color: #a5b4fc;
		border: 1px solid rgba(99, 102, 241, 0.3);
	}

	.level-connector {
		position: absolute;
		left: 39px;
		top: 60px;
		width: 2px;
		height: calc(100% + 1rem);
		background: linear-gradient(180deg, #6366f1, #8b5cf6);
	}

	/* About Card */
	.about-card {
		line-height: 1.8;
	}

	.about-card p {
		margin: 0;
		font-size: 1rem;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.hero h1 {
			font-size: 2rem;
		}

		.theory-diagram {
			flex-direction: column;
		}

		.collision {
			transform: rotate(90deg);
		}

		.level-item {
			flex-direction: column;
			align-items: center;
			text-align: center;
		}

		.level-connector {
			display: none;
		}

		.level-subjects {
			justify-content: center;
		}
	}
</style>
